1╭─ <${foo}>
 │   │     ╰─ openTagEnd(${foo})
 │   ├─ tagName.expressions[0] "${foo}"
 │   ├─ tagName.quasis[0]
 ╰─  ╰─ tagName "${foo}"
2╭─ </${foo}>
 │  │ ╰─ closeTag(${foo}).value "${foo}"
 │  ├─ text "\n"
 │  ├─ tagName.quasis[1]
 ╰─ ╰─ closeTag(${foo}) "</${foo}>"